# Milestone 10 review, round 1

Date: 2026-09-17
Scope: Subnet first/last usable with copy; `/tools/color` conversions (hex/rgb/hsl/hwb/lab/lch/oklch/cmyk), nearest names via `color-name-list/bestof`, harmonies, palette save, image sampler + loupe + k-means, EyeDropper, contrast vs washi.
Evidence pack: local gates (verified — `color.test.ts`; tools e2e subnet First/Last usable 192.168.10.129 and colour conversions; a11y on `/tools/color`).

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

The subnet usable-host rows are the missing networking fact; the colour tool stays in-browser and names colours without shipping the 5 MB full list. A 9 would add a loupe that shows magnified pixels (today it is a colour chip) and a unit test that the nearest-name path loads `bestof`.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | First/Last usable + copy | PASS | `SubnetCalculator.tsx`; `tools.spec.ts` 192.168.10.130/26 → .129 |
| 2 | Conversions + nearest names | PASS | `color.ts` `formats` / `nearestName` / `loadColorNames` → `color-name-list/bestof` |
| 3 | Palettes, sampler, k-means, EyeDropper, contrast | PASS | `ColorPicker.tsx`; `dominantPalette`; contrast vs `#f7f5ef` |

## Findings

### M10-R1-F01  Low  Code quality  Loupe is a hex chip, not a magnified crop
Location: `ColorPicker.tsx` `moveLoupe`
Problem: The plan asked for a Photoshop-style loupe; the implementation shows a filled circle of the sampled average.
Remediation direction: Draw an 8× crop from `ImageData` into a second canvas when time allows.
Status: Open — BACKLOG

## Dimension notes

### Code quality — 8
Conversions and k-means are unit-tested; e2e hits the island.

### Scalability — 8
`bestof` instead of the full JSON export keeps the Worker bundle honest.

### Reliability — 8
Parse failures stay local; no network.

### Future-proofing and migration readiness — 8
culori is the colour engine; names are a swappable import.

### Security — 8
Image sampling is canvas-only; EyeDropper is a feature detect.

### Operability — 8
Tool registry + sitemap + OG page exist.

## Operator verification required
- None.

## What was done well
- Refusing the unpublished `colornames.min.json` export and taking the package's `bestof` condition instead of vendoring a 1.2 MB ESM dump under a fake path.
