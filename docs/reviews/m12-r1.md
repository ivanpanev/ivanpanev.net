# Milestone 12 review, round 1

Date: 2026-09-17
Scope: `DrealNote` on Home and About, fed from a static curated JSON file (no live fetch of dreal.net; CSP and the self-contained site forbid it).
Evidence pack: local gates (verified — `/` and `/about` a11y + smoke; `check-dist` allows `https://dreal.net/` as an attribution link only).

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

The note is a static rotation by UTC day, attributed, and does not pull a third-party origin. The lines are original, not a scrape — that is the only honest option under the copyright rule. A 9 would date the file and say how to add a line.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | Component on Home and About | PASS | `index.astro`, `about.astro` import `DrealNote` |
| 2 | Static curated content | PASS | `apps/web/src/content/dreal.json` (7 lines) |
| 3 | No runtime fetch of dreal.net | PASS | component reads JSON at build; link is attribution only |

## Findings

### M12-R1-F01  Nit  Operability  No comment on how the rotation is seeded
Location: `DrealNote.astro` `Math.floor(Date.now() / 86_400_000)`
Problem: The pick is UTC-day modulo length; a rebuild on another day changes the shipped HTML (static).
Remediation direction: Say in the JSON that the line is chosen at build time, or pin a line if you want the live site stable between deploys.
Status: Accepted (daily rebuild is how this site ships)

## Dimension notes

### Code quality — 8
One component, one file.

### Scalability — 8
Seven strings.

### Reliability — 8
Fallback to `lines[0]`.

### Future-proofing and migration readiness — 8
Swap the JSON; no API.

### Security — 8
No new connect-src. Attribution link allowlisted in `check-dist.mjs`.

### Operability — 8
Edit `dreal.json` and rebuild.

## Operator verification required
- None.

## What was done well
- Not fetching dreal.net at runtime, which would have been both a CSP break and a copyright problem.
