# Milestone 3 review, round 3

Date: 2026-09-16
Scope: Argo CD bootstrap with KSOPS, app-of-apps and self-management Application, Cilium Gateway (NodePort/ClusterIP) plus HTTPRoutes for grafana/argocd/hubble, cloudflared ×2 behind Cloudflare Tunnel and Access, kube-prometheus-stack (Grafana JWT, Prometheus+Loki datasources, Alertmanager→ntfy via SOPS), Loki Monolithic on Hetzner Object Storage with 30-day retention, k8s-monitoring v4 logs/events, CNPG operator + Barman Cloud plugin (no Cluster CRs), cert-manager Cloudflare DNS-01 ClusterIssuer, Hubble UI via Terraform, and `k8s-validate.yml`.
Evidence pack: `docs/reviews/evidence/m3/` (verified — re-ran `bash scripts/k8s-validate.sh` (exit 0, same summaries as `k8s-validate.txt` / `kubeconform.txt`); live `kubectl` for Services/Gateway/HTTPRoute/PDB/NetworkPolicy/ClusterIssuer/Certificate/repo-server/cloudflared/Loki env+logs/secret key names/helm list; `sops filestatus` on all five `k8s/**/*.secret.yaml`; `terraform state list` for S3 resources; `curl -sI` status lines only on the four hostnames; independent ListObjectsV2 on `ivp-loki` prefixes `index/` `fake/` `probe/` (no keys printed) and LogQL `{namespace="cloudflared"}` via port-forward. Did not decrypt SOPS. Did not PutObject. Did not apply `root-application.yaml`. Could not execute: GitHub Actions remote `k8s-validate.yml` run.)

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

The platform matches the milestone: charts render and kubeconform, Gateway is Programmed with no LoadBalancer, Access 302s the three admin hosts, KSOPS repo-server matches ADR-0006, and Loki finally writes to `ivp-loki`. The round-1/2 blocker was not a bad key: Hetzner Ceph 403'd authenticated PutObject when PublicAccessBlock was set and when Loki versioning was Enabled; Terraform now omits PAB, Loki versioning is Suspended, live logs show zero Access Denied, and chunks plus `loki_index_20712` are in the bucket. Remaining items are Lows (Argo server allow-all NP, Prometheus CRD skew, Cilium kubeconform skip, ApplicationSet leftover). Align those and add the Cilium CRD schema and this is a 9.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | All Applications render and validate (`kustomize build` / `helm template` + kubeconform) | PASS | Re-ran `bash scripts/k8s-validate.sh`: invalid 0; gateway 11 valid / 1 skipped; Argo CD 41 valid / 3 skipped; kube-prometheus-stack 105 valid; Loki 11 valid; k8s-monitoring 21 valid / 1 skipped; CNPG 13 valid / 11 skipped; barman 11 valid / 1 skipped; `k8s-validate: ok`. Matches pack `docs/reviews/evidence/m3/k8s-validate.txt`. Script strips KSOPS and skips `*.secret.yaml` (`scripts/k8s-validate.sh:29-40,61`). Static `git ls-files` still only tracks `k8s/README.md`; directory builds cover the working tree. |
| 2 | Sync waves guarantee CRDs before CRs | PASS | `k8s/clusters/hetzner/applications/*.yaml`: wave 0 gateway; wave 1 cloudflared, cert-manager-issuers, kube-prometheus-stack-resources, loki-resources; wave 2 cnpg-operator (`crds.create: true` in `k8s/infrastructure/cnpg-operator/values.yaml:3-4`) + barman-cloud (`CreateNamespace=true`) + kube-prometheus-stack (`crds.enabled: false` at `k8s/infrastructure/kube-prometheus-stack/values.yaml:7-10`); wave 3 loki + k8s-monitoring; wave 4 argocd. |
| 3 | Every workload has requests/limits and a PDB where replicas > 1 | PASS | cloudflared replicas 2, requests/limits `k8s/infrastructure/cloudflared/deployment.yaml:62-68`, PDB `k8s/infrastructure/cloudflared/pdb.yaml`; live PDB minAvailable 1 currentHealthy 2, pods on ivp-w-1 and ivp-w-2. CoreDNS PDB `k8s/infrastructure/gateway/kube-system.yaml:25-34`; live `kube-system/coredns` minAvailable 1. Helm values set requests+limits for Argo CD, Prometheus, Grafana, Loki, Alloy, CNPG, Barman (`docs/reviews/evidence/m3/limits.md`). DaemonSets have no PDB by design. |
| 4 | Grafana, Argo CD, Hubble unreachable except via Tunnel + Access | PASS | Re-ran `curl -sI`: grafana/argocd/hubble HTTP/1.1 302 Found, `Www-Authenticate: Cloudflare-Access`; notes-api HTTP/1.1 404 Server cloudflare. `kubectl get svc -A`: 0 LoadBalancer, 37 ClusterIP, 1 NodePort (`cilium-gateway-public` 80:30337/TCP ClusterIP 10.0.107.114). `infra/terraform/hetzner/kubernetes.tf:31-35` CCM LBs off. Tunnel `origin_request.access.required = true` for non-public hosts (`infra/terraform/cloudflare/tunnel.tf:29-36`). Location JWTs not recorded. |
| 5 | Loki and CNPG point at S3 with credentials from SOPS (CNPG Clusters are M4; M3 is operator + Barman plugin; Loki must use Hetzner Object Storage + SOPS) | PASS | Wiring: `k8s/infrastructure/loki/values.yaml:19-43` endpoint `fsn1.your-objectstorage.com`, buckets `ivp-loki`, `${AWS_*}` + live `extraEnvFrom` secret `loki-s3` (key names only). Encrypted `loki-s3.secret.yaml` two age recipients. Live `loki-0` 1/1; CNPG operator + plugin-barman-cloud Running; `kubectl get clusters.postgresql.cnpg.io -A` empty. Independent ListObjectsV2 200: `index/loki_index_20712/...tsdb.gz`, `fake/` chunks, `probe/` objects; versioning `Suspended`. Live Loki logs `--since=20m`: AccessDenied=0, failed_flush=0, finished_uploading=11. Terraform state has buckets+versioning only; no `aws_s3_bucket_public_access_block` (`infra/terraform/hetzner/buckets.tf:22-41`). |
| 6 | Logs from a test pod visible in Grafana Explore | OPERATOR-VERIFY | Alloy logs DS 3/3 Ready, alloy-events 1/1, Loki STS 1/1, Grafana datasource CM contains Prometheus + Loki (`http://loki.loki.svc.cluster.local:3100`). Supporting LogQL via port-forward: `{namespace="cloudflared"}` 2h range `status=success` streams=6 pod=`cloudflared-7bd9bfdbc-bg2dx` sample contains `Registered tunnel connection`. Loki also rejected a 1970–now range with `limit: 30d1h`. No Access JWT here for Explore. Instruction below. |
| 7 | KSOPS + sops-age bootstrap: no SA token, read-only root, KSOPS init, `SOPS_AGE_KEY_FILE`; cluster age key never committed | PASS | `k8s/bootstrap/argocd/values.yaml:56-98`. Live repo-server: `automountServiceAccountToken=false`, `readOnlyRootFilesystem=true`, env `SOPS_AGE_KEY_FILE=/home/argocd/sops-age/keys.txt`, init `install-ksops` image digest-pinned, volumes `custom-tools` + `sops-age`. Secret `sops-age` exists (values not read). `git grep` has no `AGE-SECRET-KEY` outside runbook wording. Recipients on all five secrets: operator `age1nj2…` and `argocd_hetzner` `age1fkg…` matching `.sops.yaml:26-27`. Re-ran `sops filestatus`: all `{"encrypted":true}`. |
| 8 | Gateway ClusterIP-or-equivalent; no Hetzner LB; NodePort + ClusterIP per ADR-0005 M3 revision | PASS | `k8s/infrastructure/gateway/gatewayclass.yaml:12` `service.type: NodePort`. Live: Gateway `public` Programmed address 10.0.64.1; Service NodePort with ClusterIP 10.0.107.114. ADR-0005 revision recorded. |
| 9 | HTTPRoutes for grafana, argocd, hubble | PASS | `k8s/infrastructure/gateway/httproutes.yaml`. Live: all three Accepted=True ResolvedRefs=True; backends grafana:80, argocd-server:80, hubble-ui:80. ReferenceGrants in monitoring, argocd, kube-system. CEC `gateway/cilium-gateway-public` exists. |
| 10 | Alertmanager webhook to ntfy (OD-2) via SOPS | PASS | `k8s/infrastructure/kube-prometheus-stack/values.yaml:65-89` receiver `ntfy`, `url_file: /etc/alertmanager/secrets/alertmanager-ntfy/url`. Encrypted secret key `url`, two age recipients. Live Secret `alertmanager-ntfy` key `url`; Alertmanager STS Running. |
| 11 | k8s-monitoring v4: podLogsViaLoki, clusterEvents, metrics off, collectors filesystem-log-reader + daemonset | PASS | `k8s/infrastructure/k8s-monitoring/values.yaml:12-20,41-43`. Metrics features false. Live helm `k8s-monitoring` 4.5.2; DS `k8s-monitoring-alloy-logs` 3/3, deploy alloy-events 1/1. |
| 12 | `.github/workflows/k8s-validate.yml` runs kustomize/helm + kubeconform without decrypting SOPS | PASS | Workflow calls `bash scripts/k8s-validate.sh` (`.github/workflows/k8s-validate.yml:68-69`). Script strips KSOPS and skips secrets. Install step `sha256sum -c` for helm/kustomize/kubeconform (`k8s-validate.yml:46-62`). Local re-run exit 0. Remote GHA not executed. |
| 13 | Hubble UI enabled (Terraform `cilium_hubble_*`) | PASS | `infra/terraform/hetzner/kubernetes.tf:24-26` all true. Live: `hubble-ui` 2/2 (frontend+backend), `hubble-relay` 1/1, Service `hubble-ui` ClusterIP port 80. |
| 14 | Argo CD Helm bootstrap values + root app-of-apps + self-management Application | PASS | Values `k8s/bootstrap/argocd/values.yaml`; live helm `argocd` chart 10.9.1 app v3.5.3. Root `k8s/clusters/hetzner/root-application.yaml` not in `kustomization.yaml`. Self-manage `k8s/clusters/hetzner/applications/argocd.yaml` wave 4, ignoreDifferences on `sops-age`. Live Applications empty — runbook step 4 correctly skipped until GitHub `main` has these files (`docs/runbooks/argocd-bootstrap.md:114-125`). |
| 15 | cert-manager Cloudflare DNS-01 ClusterIssuer (wildcard may still be issuing) | PASS | `k8s/infrastructure/cert-manager-issuers/clusterissuer.yaml` solver `dns01.cloudflare` secret `cloudflare-api-token` key `api-token`. Live ClusterIssuer `letsencrypt-cloudflare` Ready True. Certificate `wildcard-ivanpanev-net` Ready False, reason DoesNotExist / Issuing certificate as Secret does not exist (allowed). |
| 16 | cloudflared Deployment ×2 with tunnel token | PASS | `deployment.yaml` replicas 2, `TUNNEL_TOKEN` from `cloudflared-tunnel`, image digest-pinned, topology spread, rollingUpdate maxSurge 0. Live 2/2 Running on ivp-w-1 and ivp-w-2. Encrypted `tunnel.secret.yaml` key `TUNNEL_TOKEN`. |
| 17 | Grafana datasources Prometheus+Loki; admin password via SOPS | PASS | Live CM `kube-prometheus-stack-grafana-datasource`: Prometheus, Alertmanager, Loki. `values.yaml:110-113` `existingSecret: grafana-admin`. Live secret keys `admin-user` / `admin-password`. JWT auth `values.yaml:125-138`. Login form disabled. |
| 18 | Loki SingleBinary/Monolithic, 30-day retention | PASS | `deploymentMode: Monolithic`, `singleBinary.replicas: 1`, `retention_period: 720h`, `retention_enabled: true` (`k8s/infrastructure/loki/values.yaml:4,44-55,61-62`). Live single STS. S3 writes succeed (row 5). Query API enforces `limit: 30d1h`. |

## Findings

### M3-R1-F01  High  Reliability  Loki cannot PutObject to Hetzner Object Storage
Location: `infra/terraform/hetzner/buckets.tf:22-41`; live `kubectl -n loki logs sts/loki -c loki --since=20m` (AccessDenied=0, finished_uploading=11); independent ListObjectsV2 200 on `ivp-loki` (`index/loki_index_20712`, `fake/` chunks); `terraform state list` has no `aws_s3_bucket_public_access_block`
Problem: Round 1/2 every observed PutObject was 403 AccessDenied, so ADR-0007 object-store retention did not hold. Cause was Hetzner Ceph mishandling PublicAccessBlock and Loki bucket versioning Enabled, not a list-only key. Same key pair PutObject 200 once PAB was deleted and Loki versioning Suspended.
Remediation direction: Keep PAB out of Terraform; leave `ivp-loki` versioning Suspended; do not re-enable either in the console. Alert on Loki Access Denied in Milestone 5.
Status: Fixed in round 3

### M3-R1-F06  Medium  Security  Entire `monitoring` namespace is PSA privileged
Location: `k8s/infrastructure/kube-prometheus-stack/resources/namespace.yaml:6-13`
Problem: node-exporter and Alloy filesystem log readers need hostPath. PSA `enforce: privileged` still admits Grafana, Prometheus, Alertmanager, and the operator in the same namespace.
Remediation direction: Keep privileged only if a dedicated host-tools namespace is worse; otherwise split exporters and leave Grafana/Prometheus at `restricted` or `baseline`.
Status: Accepted (justification: a second host-tools namespace would still be privileged and would split ServiceMonitors/RBAC on a two-worker cluster; the comment forbids adding new privileged pods. Reasonable for Phase 1.)

### M3-R1-F02  Medium  Security  Argo CD UI exec is enabled
Location: `k8s/bootstrap/argocd/values.yaml:22-25`; live `argocd-cm` `exec.enabled=false`; `kustomize.buildOptions=--enable-alpha-plugins --enable-exec --enable-helm`
Problem: `configs.cm.exec.enabled` was the UI pod-exec terminal, not the KSOPS Kustomize flag.
Remediation direction: Keep `kustomize.buildOptions --enable-exec` for KSOPS; leave UI exec off.
Status: Fixed in round 2

### M3-R1-F03  Medium  Security  k8s-validate CI installs Helm/kustomize/kubeconform without checksums
Location: `.github/workflows/k8s-validate.yml:46-62`; `scripts/versions.env:45-48`
Problem: ADR-0012 treats supply chain as a first-class control. Version-only curl was insufficient.
Remediation direction: Verify published SHA256 before install.
Status: Fixed in round 2

### M3-R1-F04  Medium  Reliability  GitOps waves race secrets (and Barman namespace) with their consumers
Location: `k8s/clusters/hetzner/applications/kube-prometheus-stack-resources.yaml:7` and `loki-resources.yaml:7` wave `"1"`; Helm consumers waves 2 and 3; `barman-cloud.yaml:25-31` `CreateNamespace=true`
Problem: Same-wave secret vs Helm left Grafana/Loki/Barman racing after handover.
Remediation direction: Keep resource Applications on an earlier wave than Helm consumers; do not apply `root-application.yaml` until `main` lists the k8s tree.
Status: Fixed in round 2

### M3-R1-F05  Medium  Security  NetworkPolicies are only on gateway and cloudflared
Location: live `kubectl get netpol -A` (argocd, cloudflared, gateway, kube-system/hubble-ui-from-gateway, loki, monitoring); `k8s/infrastructure/loki/resources/networkpolicy.yaml`; `k8s/infrastructure/kube-prometheus-stack/resources/networkpolicy.yaml`; `k8s/infrastructure/gateway/kube-system.yaml:1-23`
Problem: Inside the cluster, any pod could push/query Loki and load Hubble UI.
Remediation direction: Default-deny in loki, monitoring, and Hubble; allow Alloy→Loki, Grafana→Loki/Prometheus, cloudflared→Gateway, Prometheus scrapes.
Status: Fixed in round 2 (residual chart allow-all on `argocd-server` is M3-R2-F05)

### M3-R1-F07  Low  Reliability  CoreDNS has two replicas and no PDB
Location: `k8s/infrastructure/gateway/kube-system.yaml:25-34`; live PDB `kube-system/coredns` minAvailable 1
Problem: A worker drain could take both CoreDNS replicas.
Remediation direction: Add a CoreDNS PDB minAvailable 1.
Status: Fixed in round 2

### M3-R1-F08  Low  Operability  Evidence pack tree and kubeconform artifacts do not match MANIFEST
Location: `docs/reviews/evidence/m3/tree.txt`; `docs/reviews/evidence/m3/kubeconform.txt`
Problem: Round 1 pack claimed files that were missing or only `k8s/README.md`.
Remediation direction: Regenerate `tree.txt` from the working tree; keep one validate transcript.
Status: Fixed in round 2

### M3-R1-F09  Low  Security  Platform images pinned by tag, not digest
Location: `k8s/infrastructure/cloudflared/deployment.yaml:34`; `k8s/bootstrap/argocd/values.yaml:79`; live images match those digests
Problem: ADR-0012 wants cluster pulls by digest for the KSOPS and tunnel path.
Remediation direction: Pin both by digest (keep the tag in a comment).
Status: Fixed in round 2

### M3-R1-F10  Low  Future-proofing  Prometheus Operator CRDs (v0.93.1) vs chart operator v0.94.0
Location: live helm kube-prometheus-stack app `v0.94.0`; `k8s/infrastructure/kube-prometheus-stack/values.yaml:7-10` `crds.enabled: false`; `docs/reviews/BACKLOG.md` row `M3-R1-F10`
Problem: Live ServiceMonitors apply, so current skew is tolerated. The next chart bump can start using CRD fields the Talos extra-manifest CRDs lack.
Remediation direction: Align the module CRD pin with the chart’s operator version, or install matching CRDs from the chart once under a dedicated wave-0 Application.
Status: Open (BACKLOG)

### M3-R1-F11  Low  Code quality  CiliumGatewayClassConfig is skipped by kubeconform
Location: re-ran `scripts/k8s-validate.sh` gateway summary `Valid: 11 … Skipped: 1`; `k8s/infrastructure/gateway/gatewayclass.yaml:5-12`; script still uses `-ignore-missing-schemas` (`scripts/k8s-validate.sh:17`); `docs/reviews/BACKLOG.md` row `M3-R1-F11`
Problem: The M3 ADR revision exists because this CR rejected `ClusterIP` at apply time. CI will not catch the next typo in `spec.service.type`.
Remediation direction: Add the Cilium CRD schema to kubeconform’s `-schema-location`, or ignore-missing only for known-missing kinds, not this one.
Status: Open (BACKLOG)

### M3-R2-F01  Low  Operability  F10 and F11 deferred to BACKLOG but not recorded
Location: `docs/reviews/BACKLOG.md:18-19` now has `M3-R1-F10` and `M3-R1-F11` rows
Problem: Round 2 remediation log claimed a BACKLOG deferral that was missing, so the next milestone would not see the Lows.
Remediation direction: Add the two rows with original IDs.
Status: Fixed in round 3

### M3-R2-F02  Low  Code quality  `sops-filestatus.txt` does not cover every secret file
Location: `docs/reviews/evidence/m3/sops-filestatus.txt` (five paths, each `{"encrypted":true}`); re-ran `sops filestatus` on all five `k8s/**/*.secret.yaml`
Problem: Round 2 pack asserted five results and delivered one anonymous JSON object.
Remediation direction: Capture one filestatus line per path without decrypting.
Status: Fixed in round 3

### M3-R2-F03  Low  Operability  Validate and terraform-hubble transcripts stored as UTF-16
Location: `docs/reviews/evidence/m3/k8s-validate.txt` (now UTF-8, identical summaries to this round’s re-run); leftover `terraform-hubble-plan.txt` is still UTF-16 and not listed in the round-3 MANIFEST
Problem: Windows redirection wrote UTF-16; the validate transcript was unreadable.
Remediation direction: Re-run in a UTF-8 locale or point MANIFEST at `kubeconform.txt` only.
Status: Fixed in round 3 (stale UTF-16 Hubble plan file can be dropped from the pack)

### M3-R2-F04  Low  Security  Evidence `access-curl.txt` stores full Access login Location headers
Location: `docs/reviews/evidence/m3/access-curl.txt` (`Location:` lines now `kid=<redacted>`)
Problem: Cloudflare Access login redirects with JWT `meta=` blobs do not belong in git.
Remediation direction: Keep only status line, `www-authenticate: Cloudflare-Access`, and host; redact `location` and `set-cookie`.
Status: Fixed in round 3

### M3-R2-F05  Low  Security  Argo CD server NetworkPolicy allows all ingress
Location: live `kubectl -n argocd get netpol argocd-server` `spec.ingress: [{}]`; `k8s/bootstrap/argocd/values.yaml:7-8` `global.networkPolicy.create: true`
Problem: F05 closed Loki and Hubble. The chart default still lets any in-cluster pod hit `argocd-server`. Argo CD has its own login, so this is extra blast radius, not an unauthenticated admin UI.
Remediation direction: Restrict ingress to the Gateway identity / `gateway` and `kube-system` (Envoy) namespaces, matching Grafana’s monitoring policy.
Status: Open

### M3-R3-F01  Low  Operability  ApplicationSet controller still Running after values disable it
Location: `k8s/bootstrap/argocd/values.yaml:119-120` `applicationSet.enabled: false`; live `deploy/argocd-applicationset-controller` readyReplicas=1; helm `argocd` revision 3
Problem: Desired config turns ApplicationSet off; the bootstrap release left the controller Running. Extra process and RBAC until a helm upgrade or wave-4 self-management converges.
Remediation direction: `helm upgrade` with current values (or wait for the self-management Application after `main` exists) and confirm the ApplicationSet deploy is gone.
Status: Open

## Dimension notes

### Code quality — 8
The k8s tree is one directory per Application, chart pins live on Application `targetRevision` and `k8s/CHARTS.md`, KSOPS generators sit next to encrypted files, and CI/operator share `scripts/k8s-validate.sh`, which re-ran clean. Round 3 pack hygiene is fixed (filestatus per path, UTF-8 validate transcript, BACKLOG rows). Leftovers: README still mentions Kustomize `helmCharts` while consumption is Argo multi-source; Cilium CRs remain kubeconform-skipped (F11); `git ls-files k8s` still only `k8s/README.md` (60 untracked YAML files, expected until first push).

### Scalability — 8
Two workers, Loki Monolithic with ingestion caps (`ingestion_rate_mb: 8`), Prometheus 20Gi/7d, Alloy DaemonSet, Grafana/Argo replicas 1. ADR-0007 already accepts a Loki restart gap. S3 flush now works, so the 10Gi PVC is a buffer rather than the retention ceiling. Nothing unbounded was added. Fine for Phase 1.

### Reliability — 8
Happy path is up: Gateway Programmed, tunnel replicas with PDB and topology spread that actually landed one per worker, operators Ready, CoreDNS PDB, secret Applications on wave 1. Object storage now holds chunks and index; Loki logs have zero Access Denied since the STS restart; LogQL returns cloudflared streams; the query API enforces a ~30-day window. Remaining pain is operational discipline around PAB/versioning (encoded in `buckets.tf` and the runbook) and the still-issuing wildcard cert (allowed). No open High.

### Future-proofing and migration readiness — 8
ADR-0005 NodePort revision matches the live Service. ADR-0006 KSOPS trust boundary is implemented in the live repo-server. ADR-0007 Monolithic rename and ntfy (OD-2) are in values. S3 endpoint and bucket names stay in values; the Hetzner Ceph PAB/versioning workaround is isolated to `buckets.tf`. Prometheus Operator CRD skew (F10) and GitHub `main` not yet containing these files remain the migration nits.

### Security — 8
Edge story holds: Access 302, no LoadBalancer, SOPS files encrypted for operator + cluster age, repo-server has no SA token and a read-only root, Grafana login form off + JWT, UI exec off, CI tool checksums, images digest-pinned, Loki no longer world-reachable in-cluster. PAB removal is a documented Hetzner-private-by-default workaround, not an AWS-style public bucket. Remaining: accepted privileged PSA on `monitoring`, chart allow-all on `argocd-server`, Hubble origin still needs an authenticated check.

### Operability — 8
`docs/runbooks/argocd-bootstrap.md` names the Loki Access Denied / PAB / versioning failure mode, skip-root-until-main, and rollback that leaves cert-manager alone. Cluster facts and CHARTS.md match the live helm list. BACKLOG now carries F10/F11. Explore still needs an Access JWT. ApplicationSet leftover will confuse the first `argocd app list` after handover until values converge.

## Operator verification required
- Authenticate to Grafana with Cloudflare Access. Explore → Loki. Run `{namespace="cloudflared"}` and confirm recent lines (in-cluster LogQL already succeeds; this is the Explore/JWT path).
- After Access login, confirm grafana/argocd/hubble origins actually serve (302 only proves the edge challenge). Do not paste Location JWTs into tickets.
- Wait until `certificate/wildcard-ivanpanev-net` is Ready, or capture the Challenge error if DNS-01 stays stuck.
- Do not `kubectl apply -f k8s/clusters/hetzner/root-application.yaml` until GitHub `main` contains `k8s/infrastructure/**` and `k8s/clusters/hetzner/**`. Confirm with `argocd app list` after handover.
- First push of `k8s/` should show a green `k8s-validate` and `hygiene / sops files are encrypted` run.
- Do not re-create PublicAccessBlock or set `ivp-loki` versioning to Enabled; that recreates F01.

## What was done well
- F01 was chased to a provider bug with a controlled experiment (fresh bucket PutObject 200 vs `ivp-loki` 403, versioning toggle reproducing 403, then Suspend restoring 200) instead of rotating keys blindly; Terraform `state rm` plus deleting the PAB resource means the next apply cannot recreate the failure mode.
- Repo-server still matches ADR-0006 live: no automounted SA token, read-only root, KSOPS init `runAsUser: 65532` on a digest-pinned image, `SOPS_AGE_KEY_FILE`, age Secret not in Git.
- Independent ListObjectsV2 shows Loki-written `fake/` chunks and `index/loki_index_20712`, and LogQL `{namespace="cloudflared"}` returns streams — retention is no longer a config comment.
- `scripts/k8s-validate.sh` is the same path CI will run, strips KSOPS without decrypting, and reproduced the pack (exit 0).
- cloudflared digest pin plus `maxSurge: 0` is the right pairing with `DoNotSchedule` topology spread; live replicas are on separate workers.

## Escalation (round 3 failures only)
Not applicable — this round passes. Unresolved Lows (M3-R1-F10, M3-R1-F11, M3-R2-F05, M3-R3-F01) stay in BACKLOG / the next milestone; they do not block.
