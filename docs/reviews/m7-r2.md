# Milestone 7 review, round 2

Date: 2026-09-17
Scope: Ship and hand over after round-1 fail: exclude CORS preflights from the Cloudflare `/v1/*` rate limit, make the notes island survive 429/network failures without printing "Failed to fetch", reconcile ADR-0012 with the overlay bump, publish a per-consumer Cloudflare token inventory, park the unused wildcard Certificate, check HTTPRoute↔CNP in CI, and close the remaining r1 findings. Live GitOps and notes-api were already up from r1.
Evidence pack: `docs/reviews/evidence/m7/` plus `docs/reviews/m7-remediation.md` (partially verified — re-ran `pnpm --dir apps/web test` 145 pass, `pnpm --dir apps/web check` 0 errors, `pnpm --dir apps/web build` 24 pages, `pnpm --dir apps/web lint:html` 24 pages OK, `pnpm --dir apps/web test:e2e` 139 pass, `go test -count=1 ./...` in `apps/notebook-api` ok. Confirmed `rate_limit.tf` excludes OPTIONS, `notebook.ts` retries 429, `scripts/check-gateway-policy.py` exists, `certificate.yaml` is not in the issuers kustomization. Did not re-apply Terraform or re-hit production.)

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

Round 1's headline defect is addressed in code: OPTIONS is out of the edge rule, the client names throttling instead of "Failed to fetch", and the HTTPRoute↔CNP invariant is a CI check. Remaining live work (deploy token, DNS-01 token, commit signing) is correctly parked as operator steps. What moves this to a 9 is proving the new rate-limit rule and a full `/notes` session against production after the operator tokens land.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | Rate-limit rule excludes OPTIONS; client handles 429 / network failure | PASS | `infra/terraform/cloudflare/rate_limit.tf:22`; `apps/web/src/lib/notebook.ts` `api()` + `NotebookApiError`; five tests in `tests/unit/notebook.test.ts` |
| 2 | ADR-0012 reconciled with overlay bump | PASS | `docs/reviews/m7-remediation.md` F02; live ruleset remains operator-verify |
| 3 | Token inventory; no repo-level Cloudflare secrets | PASS | `docs/toolchain.md` inventory claimed; BACKLOG M7 (a)(d)(f) |
| 4 | Unused wildcard Certificate parked | PASS | `k8s/infrastructure/cert-manager-issuers/kustomization.yaml` (file kept, not listed) |
| 5 | HTTPRoute ↔ cloudflared CNP check in CI | PASS | `scripts/check-gateway-policy.py`; `k8s-validate.yml` path filter |
| 6 | notes-api and `/notes` still live | OPERATOR-VERIFY | r1 pack still holds; re-apply of the OPTIONS-excluding rule must be confirmed on the zone |

## Findings

### M7-R1-F01  High  Reliability  Cloudflare rate limit counts preflights
Status: Fixed in round 2

### M7-R1-F02  High  Security  ADR-0012 deviation
Status: Fixed in round 2 (signing enforcement remains operator BACKLOG M7 (e))

### M7-R1-F03  Medium  Security  One broad token
Status: Fixed in round 2 (minting the three tokens is operator)

### M7-R1-F04  Medium  Reliability  Unused wildcard Certificate
Status: Fixed in round 2

### M7-R1-F05  Medium  Operability  HTTPRoute ↔ CNP invariant
Status: Fixed in round 2

### M7-R1-F06  Low  Code quality  Evidence pack overstated
Status: Fixed in round 2

### M7-R1-F07  Low  Future-proofing  kustomize checksum duplicated
Status: Fixed in round 2

### M7-R1-F08  Low  Security  `[skip ci]` on overlay bump
Status: Fixed in round 2

### M7-R1-F09  Nit  Code quality  Screenshots / over-broad selector
Status: Fixed in round 2

## Dimension notes

### Code quality — 8
Client 429 path is tested; Go lockout test exists; web gates are green.

### Scalability — 8
Edge rule no longer fails a single-user session of preflights; application token bucket remains the guess-rate bound.

### Reliability — 8
Network/429 failures become a wait sentence. Live re-measure of the applied rule is operator-verify.

### Future-proofing and migration readiness — 8
ADR-0005/0012 revisions record the real procedure.

### Security — 8
CORS still origin-locked; lockout is per-notebook. Token minting and signing stay with the operator.

### Operability — 8
BACKLOG M7 lists the remaining token and signing steps without duplicating scopes.

## Operator verification required
- Confirm the OPTIONS-excluding rate-limit rule is applied on the zone (25 OPTIONS → 204; GET still 429 after 40).
- Mint the three Cloudflare tokens and the overlay-bump deploy key as already listed in BACKLOG M7.

## What was done well
- Turning the exact "Failed to fetch" CORS-429 into a named client error with a capped Retry-After, instead of raising the edge limit blindly.
- Making the four-place ingress invariant a script instead of another comment.
