# Milestone 7 remediation (round 1 → 2)

Commits are on `main`; the round-2 evidence pack (`docs/reviews/evidence/m7/`)
was regenerated after these changes.

| ID | Change |
| --- | --- |
| M7-R1-F01 | Edge: `rate_limit.tf` expression excludes `OPTIONS`; `rate_limit_requests` default 40 (validation `>= 20`), applied with `terraform apply` from the operator machine (plan: one in-place ruleset change). Measured afterwards: 25 preflights → 25× 204; 46 GETs → 40× 200 then 429. Client: `notebook.ts` `api()` retries a 429 once after `Retry-After` (capped at `MAX_RETRY_AFTER_MS`), surfaces `NotebookApiError` with the wait time, and turns a network-level `TypeError` into an actionable sentence; five unit tests in `tests/unit/notebook.test.ts`. `notebook-threat-model.md` updated with the measured limits. BACKLOG M4-R2-F03 closed by this. |
| M7-R1-F02 | GitHub ruleset `main` (id 23569959): no deletion, no force-push, linear history, required checks = the four `hygiene` jobs. Bypass actor: deploy keys only (GitHub refuses the Actions app on a personal repo). `notebook-api.yml`: bump moved to its own `bump` job that pushes over SSH with a write deploy key from the new `overlay-bump` environment; every `GITHUB_TOKEN` in the workflow is `contents: read`. ADR-0012 revision 2026-09-17 records the mechanism, why a bump PR cannot work, and that signing enforcement waits on the operator's signing key (BACKLOG M7 (e)). Runbook "Preconditions" and BACKLOG restored the branch-protection note. |
| M7-R1-F03 | `docs/toolchain.md` "Cloudflare API tokens" is the single inventory: three consumers, three tokens, exact scopes, resources, IP-filter rule. `web.yml`, `apps/web/README.md`, `argocd-bootstrap.md` and the BACKLOG point there instead of listing scopes. Confirmed via API that no repository-level copies of the secrets exist (`/actions/secrets` → 0). BACKLOG M7 (f): roll the operator token after the swap. |
| M7-R1-F04 | `certificate.yaml` removed from `cert-manager-issuers/kustomization.yaml` (file kept, comment explains re-enable condition); Argo CD prunes the Certificate, Order and Challenge. Runbook: delete an auth-failing Challenge rather than let it retry. |
| M7-R1-F05 | `scripts/check-gateway-policy.py` (PyYAML) asserts, for every HTTPRoute backend namespace, a cloudflared `toEndpoints` rule and a `fromEntities: [ingress]` CNP exist and agree on a container port, and flags stale cloudflared rules; wired into `k8s-validate.sh` and the workflow path filters (`python3-yaml` installed from the Ubuntu archive if absent). ADR-0005 consequence rewritten and a 2026-09-17 revision records the four-step procedure, the 403 symptom, and the check's static limits. |
| M7-R1-F06 | `notes-roundtrip.mjs` now opens the item in the second context, asserts the decrypted plaintext equals what was stored, deletes it, and exits 1 on mismatch. Evidence files written as UTF-8. MANIFEST no longer claims repository-level secrets. |
| M7-R1-F07 | `PIN_KUSTOMIZE_SHA256_LINUX_AMD64` in `scripts/versions.env`; both workflows read it. |
| M7-R1-F08 | `[skip ci]` removed from the bump commit; hygiene and k8s-validate run on it (the deploy-key push raises normal events). |
| M7-R1-F09 | `/Screenshot_*.png` ignored; restore CNP scoped to `cnpg.io/cluster: notebook-restore`; dead `namespaceSelector: gateway` ingress rule removed from `notebook-api` NetworkPolicy with a comment pointing at the CNP. |
