# Milestone 8 review, round 1

Date: 2026-09-17
Scope: Zen visual system: washi/sumi canvas, three accents (koke, murasaki, kaki), Zen Old Mincho + Zen Kaku Gothic New via the Fonts API, 2px radius, hairline cards, hanko seal in the footer, ThemeToggle/OG/tests updated. Ember/neon skins removed.
Evidence pack: local gates (verified — `pnpm --dir apps/web test` 145, `check` 0, `build` 24 pages, `lint:html` 24, `test:e2e` 139 including theme + a11y light/dark desktop and mobile).

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

The token layer is one canvas and three named skins, fonts are self-hosted, and axe no longer fails contrast on kaki links or dark fill chips. The single most important leftover is that kaki's link colour had to be darkened past the requested #cc700e for AA body text — fill still uses the orange family. A 9 would pin measured contrast ratios next to each token in `skins.css`.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | Skins koke / murasaki / kaki on washi + sumi | PASS | `apps/web/src/lib/theme.ts` `SKINS`; `skins.css` |
| 2 | Zen Old Mincho + Zen Kaku Gothic New via Fonts API | PASS | `apps/web/astro.config.ts` fonts; `about.astro` colophon |
| 3 | ThemeToggle, OG, theme/smoke/a11y | PASS | `ThemeToggle.tsx`; `og.ts`; `theme.spec.ts`; a11y 139 e2e |
| 4 | Hanko in footer | PASS | `Footer.astro` + `.hanko` (dark vermilion `#f2a090`) |

## Findings

### M8-R1-F01  Low  Accessibility  Kaki link token is darker than the requested #cc700e
Location: `apps/web/src/styles/skins.css` `--accent: #7a3f04` (light kaki)
Problem: #cc700e and even #b05f06 on #f7f5ef fail 4.5:1 (axe measured 4.29 for #b05f06). Fill `#cc700e` remains as `--swatch-kaki`.
Remediation direction: Keep the AA text token; document the swatch/fill vs text split in the colophon.
Status: Accepted (AA over the requested hex for body/link text)

## Dimension notes

### Code quality — 8
Head script still hashed from `buildHeadScript()`; skin ids covered in unit + e2e.

### Scalability — 8
CSS tokens only; no extra runtime.

### Reliability — 8
Storage-blocked first paint still sets theme and skin.

### Future-proofing and migration readiness — 8
Three skins, one canvas; adding a fourth is a token file + `SKINS` row.

### Security — 8
No new network origins; fonts are fontsource/`'self'`.

### Operability — 8
Colophon states the three names a visitor will see in the toggle.

## Operator verification required
- None beyond shipping the build.

## What was done well
- Dark fill chips use light `--accent-fg` so `bg-accent-fill text-accent-fg` is AA on every skin, not only koke.
