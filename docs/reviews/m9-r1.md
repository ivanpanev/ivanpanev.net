# Milestone 9 review, round 1

Date: 2026-09-17
Scope: Quick PIN (generated 10-char code + PIN ≥4, salt `ivanpanev.net/notebook/pin/v1`), exclusive lifetimes 3m / 8m / 18m / 38m / 1h18m / 2h 38m / 5h18m, API MIN/MAX/DEFAULT TTL, per-notebook auth lockout (10 / 15m → 429), `workspace` kind, ADR-0009 amendment.
Evidence pack: local gates (verified — web 145 unit + 139 e2e including exclusive TTL menu and short-PIN reject; `go test ./...` including `TestAuthLockoutAfterFailures`; config defaults 3m / 5h18m / 18m).

## Verdict

Overall: 8 — PASS
Code quality: 8 | Scalability: 8 | Reliability: 8 | Future-proofing: 8 | Security: 8 | Operability: 8

PIN-only was correctly rejected: the secret is `code || NUL || PIN` under a distinct salt, so a 4-digit PIN is not a 10⁴ address space. The lifetime menu is exactly the seven strings and nothing else. Lockout is per notebook id, including DELETE (via GetItem → notebook id). A 9 would add a Playwright burst against a mocked 429 and a Postgres lockout integration test.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| - | --------- | ------ | -------- |
| 1 | Code + PIN, not PIN-only | PASS | `derivePinKeys`, `generateNotebookCode`, `Notes.tsx` Quick PIN tab |
| 2 | Exclusive TTL list | PASS | `TTL_OPTIONS` labels `3m, 8m, 18m, 38m, 1h18m, 2h 38m, 5h18m`; notes + editor e2e |
| 3 | API TTL defaults and clamp | PASS | `config.go` 3m / 5h18m / 18m; Deployment env; `config_test.go` |
| 4 | Lockout 10 fails / 15m | PASS | `http.go` `authFailMax`; `TestAuthLockoutAfterFailures`; migration `002_auth_attempts_workspace.sql` |
| 5 | ADR-0009 + threat model | PASS | `docs/adr/0009-notebook-crypto-and-language.md`; `notebook-threat-model.md` |

## Findings

### M9-R1-F01  Low  Code quality  httptest lockout uses injected 1h–7d caps
Location: `apps/notebook-api/internal/api/http_test.go` `testServer` MinTTL/MaxTTL
Problem: Handler tests still inject the old hour/week window; production defaults are covered only in `config_test.go`.
Remediation direction: Point one httptest at the env defaults so a 24h PUT is 400.
Status: Open — BACKLOG

## Dimension notes

### Code quality — 8
PIN helpers and exclusive TTL list have unit tests; island e2e covers both modes' rejects.

### Scalability — 8
Lockout rows are per notebook, not per IP; edge + token bucket still bound guess rate.

### Reliability — 8
Delete lockout keys on the notebook, not the item UUID.

### Future-proofing and migration readiness — 8
`workspace` kind is in ParseKind, OpenAPI, and the migration CHECK.

### Security — 8
Separate PIN/editor salts; lockout after 10 wrong `X-Auth`. A leaked code still needs the PIN and then hits lockout.

### Operability — 8
`.env.example` and Deployment agree on 3m / 5h18m / 18m.

## Operator verification required
- After image bump: confirm a 10th wrong X-Auth on a live notebook returns 429 with Retry-After.

## What was done well
- Refusing PIN-only after the 10k-enum observation, and keeping the UI lifetimes as an exclusive list rather than a free duration field.
