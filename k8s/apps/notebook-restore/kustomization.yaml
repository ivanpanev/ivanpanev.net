apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Manual restore-drill only. Do not add this directory to
# k8s/clusters/hetzner — Argo must not create a second Postgres that
# shares the live WAL prefix.
resources:
  - namespace.yaml
  - objectstore.yaml
  - cluster.yaml
  - networkpolicy.yaml
